interface:
  display_name: "Ship It"
  short_description: "Open, validate, merge, and clean up a PR"
  default_prompt: "Use $ship-it to ship my current changes, merge the green PR, and clean up its branch."
